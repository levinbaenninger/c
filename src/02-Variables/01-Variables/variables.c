#include <stdio.h>

int main() {
    // Variables are containers for storing data values.
    // In C, we can declare variables by specifying the data type and the name of the variable.

    int num1 = 10;
    char _character = 'a';

    // You can also declare multiple variables of the same type in one line:
    int num2 = 20, num3 = 30;

    // You can also declare variables without initializing them:
    int num4, num5;

    // You can also change the value of a variable:
    num1 = 40;
}