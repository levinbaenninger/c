#include <stdio.h>

int main() {
    // Constants are like variables, except that their value cannot be changed after their definition.
    // In C, we can declare constants by using the const keyword.

    const int PI = 3.14;
    const char _CHARACTER = 'a';
}